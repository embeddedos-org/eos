# EoS Example: POSIX App
project:
  name: posix-app
  version: 0.1.0

workspace:
  backend: ninja
  build_dir: .eos/build

toolchain:
  target: host

system:
  kind: rtos
  entry: main.c
  output: posix-app
