# EoS Example: BLE Sensor
project:
  name: ble-sensor
  version: 0.1.0

workspace:
  backend: ninja
  build_dir: .eos/build

layers:
  - layers/core
  - layers/bsp/nrf52

toolchain:
  target: arm-none-eabi

system:
  kind: rtos
  entry: main.c
  output: ble-sensor.bin
